include<vector>
#include <opencv2/calib3d.hpp>
    class Projection {
    void DrawAxes(cv::Mat& image, ) {
        //        cv::line(frame, corners[0], corners[1], cv::Scalar(255,0,0),
        //        5);
    }
};